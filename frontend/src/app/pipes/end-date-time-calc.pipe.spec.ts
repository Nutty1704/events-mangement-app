import { EndDateTimeCalcPipe } from './end-date-time-calc.pipe';

describe('EndDateTimeCalcPipe', () => {
  it('create an instance', () => {
    const pipe = new EndDateTimeCalcPipe();
    expect(pipe).toBeTruthy();
  });
});
